import styled from 'styled-components';

export const StyledShopMain = styled.main`
  width: 100%;
  height: 100%;
  display: flex;
  flex-direction: column;
  overflow: hidden;

  section:nth-child(3) {
    margin-top: 64px;
  }
`;
